-- config: (lint (only doc:malformed-tag doc:malformed-type doc:unknown-flag doc:unknown-tag doc:duplicate-definitions doc:bad-index doc:wrong-tag))

--- A module
--
-- @module foo
-- @module bar
-- @module[x]

--- @unknown_standalone
