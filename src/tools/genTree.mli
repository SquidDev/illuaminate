(** A half-baked code generator for the syntax tree.

    This isn't designed to generate fully working code, but rather write 90% of the boilerplate for
    us. It doesn't actually generate working code, and so isn't baked into the build script. *)
