((body (name foo)
  (contents
   (table
    (a
     ((description
       "A basic variable\n @usage\n\n```lua\n  local foo = require \"foo\"\n  print(foo.a)\n  \n```\n")
      (body expr (ty ((name number))) (value 1))))))))