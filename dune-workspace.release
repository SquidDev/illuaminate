(lang dune 2.0)
(profile release)

(context
 (opam
  (switch 4.08.1)
  (targets native windows)))

(context
 (opam
  (switch 4.08.1+musl+static+flambda)
  (env
   (release
   (ocamlopt_flags :standard -ccopt -static)))))
